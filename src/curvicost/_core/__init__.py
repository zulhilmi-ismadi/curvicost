"""Vendored analysis engine, kept byte-identical to the study's `code/p16/`.

These modules generated the paper's results. They are copied here rather than
re-implemented so the tool and the paper cannot drift; `tests/test_core_sync.py`
fails if they do. Treat this subpackage as read-only: edit `code/p16/` and
re-sync.
"""
